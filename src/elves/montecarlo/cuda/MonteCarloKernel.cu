void compute(size_t reiterations, size_t dimension, int* playfield, unsigned int* moveX, unsigned int* moveY, unsigned int* wins, unsigned int* visits)
{
	*moveX = 1U;
	*moveY = 8U;
	*wins = 1U;
	*visits = 9U;
}