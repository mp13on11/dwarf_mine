INCLUDE_DIRECTORIES(
    ${CMAKE_CURRENT_SOURCE_DIR}/
)

FILE(GLOB SHARED_MATRIX_ELF_SOURCES
    "*.h"
    "*.cpp"
)

ADD_SUBDIRECTORY(cuda)
ADD_SUBDIRECTORY(smp)

MESSAGE(STATUS "TEST: ${CUDA_MATRIX_ELF_LIB}")
